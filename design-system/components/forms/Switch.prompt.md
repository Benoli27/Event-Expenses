Toggle for immediate settings (notifications, reminders).

```jsx
<Switch id="emails" label="Email me about camps" checked={on} onChange={…} />
```

Inside a form that gets submitted, use `<Checkbox>` instead.

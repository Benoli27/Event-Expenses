Functional UI icon, loaded from the Lucide CDN — a flagged substitution, since the Scout brand defines no icon set.

```jsx
<Icon name="chevron-right" size={18} />
```

Never mix icon families. Never use emoji. For anything that should read as *Scouts* rather than as UI, use `<Fleur />`.

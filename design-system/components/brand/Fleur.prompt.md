The fleur-de-lis alone — the only glyph the brand actually owns.

```jsx
<Fleur size={20} tone="purple" />
<Fleur size={420} tone="white" opacity={0.12} />
```

Single colour, never multicolour. Only use it without the "Scouts" wordmark when the Scout context is already obvious.

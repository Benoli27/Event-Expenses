Icon-only control for nav toggles, close and pagination.

```jsx
<IconButton icon="menu" label="Open menu" variant="ghost" />
```

Always pass `label`. Keep at 44px or larger. `inverse` when placed on purple or on a photograph.

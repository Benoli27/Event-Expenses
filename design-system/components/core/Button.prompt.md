The call to action — bold Nunito Sans, sentence case, 4px corners.

```jsx
<Button variant="primary" size="lg" href="/join">Join the adventure</Button>
```

`primary` purple and `secondary` teal on white; `inverse` (white) or `outline` when sitting on a colour field or photograph. Labels are short verb phrases in sentence case — "Get involved", not "GET INVOLVED".

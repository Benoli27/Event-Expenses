Modal for a single decision or a short form.

```jsx
<Dialog title="Book a taster session" onClose={close} footer={<><Button variant="ghost">Cancel</Button><Button>Book</Button></>}>
  Come along on a Monday and see what we do.
</Dialog>
```

Positions absolutely inside the nearest positioned ancestor, so a screen mock can contain it.

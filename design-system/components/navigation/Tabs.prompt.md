Tab row for switching views within a page.

```jsx
<Tabs items={['Sections','What we do','Join us']} value={tab} onChange={setTab} />
```

Labels are sentence case nouns or short phrases. Use `tone="inverse"` on a purple band.
